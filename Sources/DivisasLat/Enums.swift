import Foundation

public enum Country: String, Codable {
    case GT, MX, CR, CO, PE, CL, AR, BR, DO, HN, NI, SV, PA
}

public enum Currency: String, Codable {
    case USD, EUR, GTQ, MXN, CRC, COP, PEN, CLP, ARS, BRL, DOP, HNL, NIO, SVC, PAB
}
